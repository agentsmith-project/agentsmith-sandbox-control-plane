apiVersion: v1
kind: Service
metadata:
  name: agentsmith-sandbox-control-plane-nodeport
  namespace: sandbox-system
spec:
  type: NodePort
  selector:
    app: agentsmith-sandbox-control-plane
  ports:
  - name: http
    port: 80
    targetPort: 8080
    nodePort: 30080
