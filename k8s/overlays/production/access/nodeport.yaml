apiVersion: v1
kind: Service
metadata:
  name: sandbox-manager-nodeport
  namespace: sandbox-system
spec:
  type: NodePort
  selector:
    app: sandbox-manager
  ports:
  - name: http
    port: 80
    targetPort: 8080
    nodePort: 30080
