# Production Private Access Examples

These manifests are not part of the default production kustomization.

Use them only as a private operator opt-in after reviewing network policy, authentication, and cluster routing. The default production overlay keeps ASBCP reachable through the internal ClusterIP service only.
