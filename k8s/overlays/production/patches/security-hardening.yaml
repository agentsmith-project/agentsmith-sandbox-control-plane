apiVersion: apps/v1
kind: Deployment
metadata:
  name: agentsmith-sandbox-control-plane
  namespace: sandbox-system
spec:
  template:
    spec:
      containers:
      - name: asbcp
        imagePullPolicy: Always  # 生产环境总是拉取最新镜像
        securityContext:
          readOnlyRootFilesystem: false  # ASBCP 当前需要写入临时运行数据
          runAsNonRoot: true
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
