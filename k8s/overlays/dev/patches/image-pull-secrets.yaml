apiVersion: apps/v1
kind: Deployment
metadata:
  name: agentsmith-sandbox-control-plane
  namespace: sandbox-system
spec:
  template:
    spec:
      imagePullSecrets:
      - name: harbor-registry-secret
