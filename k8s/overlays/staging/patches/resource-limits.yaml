apiVersion: apps/v1
kind: Deployment
metadata:
  name: agentsmith-sandbox-control-plane
  namespace: sandbox-system
spec:
  template:
    spec:
      containers:
      - name: asbcp
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "1"
            memory: "1Gi"
