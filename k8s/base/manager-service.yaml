apiVersion: v1
kind: Service
metadata:
  name: sandbox-manager
  namespace: sandbox-system
spec:
  selector:
    app: sandbox-manager
  ports:
  - name: http
    port: 80
    targetPort: 8080
  type: ClusterIP
