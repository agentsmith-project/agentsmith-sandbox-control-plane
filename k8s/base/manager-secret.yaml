apiVersion: v1
kind: Secret
metadata:
  name: sandbox-manager-keys
  namespace: sandbox-system
type: Opaque
stringData:
  # Example service keys - CHANGE THESE IN PRODUCTION
  # You can specify multiple keys for rotation (comma-separated)
  SERVICE_KEYS: "dev-key-12345,prod-key-67890"
