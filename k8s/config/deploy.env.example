# K8s 部署配置
# 复制此文件为 deploy.env 并修改

# 部署环境
ENVIRONMENT=dev

# Registry 配置
REGISTRY=harbor.pullot.com:28443
HARBOR_PROJECT=agentsmith

# Harbor 认证（可选，如果使用 secret 则不需要）
REGISTRY_USERNAME=admin
REGISTRY_PASSWORD=

# 镜像版本（可选，默认从 VERSION 文件读取）
# ASBCP_VERSION=2.0.9
# RUNNER_VERSION=1.0.0
