# mbos-sandbox V06 收口检查单

**检查日期**: 2026-02-10
**版本**: V06
**检查目标**: 确保代码无明显bug和错误，达到上线稳定运行的标准

---

## 检查概述

本次检查涵盖 mbos-sandbox 项目的核心代码模块，重点关注可能导致生产环境问题的明显bug和错误。检查标准务实，不吹毛求疵，以确保系统稳定运行为首要目标。

### 检查范围
- manager-service (Go HTTP/WebSocket API 服务)
- shell-bridge (WebSocket PTY shell 服务器)
- k8s 集成模块
- 存储客户端
- 会话和缓冲管理
- 认证和限流

---

## 1. 并发安全问题

### 1.1 session/manager.go - ✅ 良好
- 所有公共方法使用 `sync.RWMutex` 保护
- `GetOrCreate` 方法原子性操作，防止竞态条件
- `cleanupExpired` 方法持有锁期间操作map，安全

### 1.2 buffer/manager.go - ✅ 良好
- `GetOrCreate` 和 `Delete` 方法正确使用锁保护
- 注意：返回的 `RingBuffer` 本身不是并发安全的（文档已警告）

### 1.3 buffer/ring.go - ✅ 良好
- `Write`, `ReadAll`, `Clear` 方法都使用 `sync.RWMutex`
- `Add` 方法有消息大小验证（1MB限制）

### 1.4 websocket/handler.go - ⚠️ 需要注意
**位置**: `websocket/handler.go:94-110`
```go
var cleanupMu sync.Mutex // Mutex to protect cleanupDone flag
cleanupDone := false     // Flag to prevent double cleanup
```
- 使用 mutex 保护 cleanup 标志是正确的
- defer cleanup 逻辑正确处理了竞态条件

### 1.5 shellbridge/client.go - ✅ 良好
- `connMu` 保护连接状态
- `ExecCommand` 和 `ReceiveOutput` 正确使用读锁

### 1.6 ratelimit/limiter.go - ✅ 良好
- 使用 `sync.Map` 实现无锁并发访问
- `limiterEntry.mu` 保护 lastAccess 字段
- `Stop()` 方法有 stoppedFlag 防止重复关闭

### 1.7 shell-bridge/internal/server/server.go - ✅ 良好
- session 操作使用 mutex 保护

### 1.8 shell-bridge/internal/pty/session.go - ✅ 良好
- 所有 PTY 操作都使用 mutex 保护
- `closed` 标志防止重复关闭

---

## 2. 资源泄漏风险

### 2.1 HTTP 请求体关闭 - ✅ 已处理
**位置**: `httpapi/handlers.go`
- `HandleCreateSandbox`: `defer r.Body.Close()` (line 62)
- `HandleExec`: `defer r.Body.Close()` (line 180)
- `HandleUpload`: `defer r.Body.Close()` (line 310)

### 2.2 存储流关闭 - ✅ 已处理
**位置**: `websocket/handler.go:334-338`
```go
defer func() {
    if closeErr := tarData.Close(); closeErr != nil {
        h.logger.Warn("Failed to close snapshot data stream...")
    }
}()
```

### 2.3 WebSocket 连接关闭 - ✅ 已处理
**位置**: `websocket/handler.go:80`
```go
defer conn.Close()
```

### 2.4 Shell-bridge 客户端关闭 - ✅ 已处理
**位置**: `websocket/handler.go:403`
```go
defer client.Close()
```

### 2.5 PTY 会话关闭 - ✅ 已处理
**位置**: `shell-bridge/internal/pty/session.go`
- `Close()` 方法正确处理 PTY 和进程清理

### 2.6 下载流关闭 - ✅ 已处理
**位置**: `httpapi/handlers.go:429-433`
```go
defer func() {
    if closeErr := stream.Close(); closeErr != nil {
        log.Printf("[WARN] Failed to close download stream...")
    }
}()
```

---

## 3. 错误处理和边界情况

### 3.1 Context 取消检测 - ✅ 良好
**位置**: `httpapi/handlers.go:628-641`, `websocket/handler.go:666-679`
```go
func isContextCanceled(err error) bool {
    if err == nil {
        return false
    }
    if err == context.Canceled || err == context.DeadlineExceeded {
        return true
    }
    errMsg := err.Error()
    return contains(errMsg, "context canceled") ||
        contains(errMsg, "operation was canceled") ||
        contains(errMsg, "deadline exceeded")
}
```

### 3.2 Nil 指针保护 - ✅ 已处理
**位置**: `shellbridge/client.go:66-72`
```go
c.connMu.RLock()
if !c.connected || c.conn == nil {
    c.connMu.RUnlock()
    return fmt.Errorf("not connected to shell-bridge")
}
```

### 3.3 消息大小限制 - ✅ 已处理
**位置**: `websocket/handler.go:545-549`
```go
maxEncodedSize := (h.cfg.WebSocket.MaxMessageSize * 4) / 3
if int64(len(payload.Data)) > maxEncodedSize {
    h.logger.Error("Stdin data too large...")
    continue
}
```

### 3.4 超时处理 - ✅ 已处理
**位置**: `websocket/handler.go:113`
```go
conn.SetReadDeadline(time.Now().Add(30 * time.Second))
```

### 3.5 存在性检查 - ✅ 已处理
**位置**: `k8s/pods.go:208-237`
```go
// Try to get existing pod
pod, err := c.clientset.CoreV1().Pods(c.namespace).Get(ctx, podName, metav1.GetOptions{})
if err == nil {
    // Pod exists...
}
if !errors.IsNotFound(err) {
    return nil, fmt.Errorf("failed to get pod: %w", err)
}
// Create new pod...
```

### 3.6 Finalizer 处理 - ⚠️ 潜在问题
**位置**: `k8s/finalizer.go:33-40`
```go
if errors.IsNotFound(err) {
    // Pod is already gone, nothing to do
    log.Printf("K8s: pod %s/%s not found when removing finalizer", namespace, podName)
    return nil
}
```
- 这是正确的处理：当 pod 已经不存在时，不应该返回错误

---

## 4. 数据一致性问题

### 4.1 Pod 状态同步 - ✅ 良好
- `EnsurePod` 方法正确处理了竞态条件（line 243-254）
- `PatchActivity` 使用 merge patch，原子更新

### 4.2 会话状态管理 - ✅ 良好
- `MarkClientConnected`/`MarkClientDisconnected` 正确更新状态和过期时间
- `IsExpired()` 考虑了连接状态

### 4.3 缓冲区并发写入 - ✅ 良好
- `RingBuffer.Write` 使用锁保护
- `forwardIO` 方法正确使用 channel 进行 goroutine 间通信

---

## 5. 网络和协议问题

### 5.1 WebSocket 握手超时 - ✅ 已配置
**位置**: `shellbridge/client.go:16`
```go
const HandshakeTimeout = 10 * time.Second
```

### 5.2 二进制帧解析 - ✅ 有大小限制
**位置**: `shell-bridge/internal/protocol/frame.go:35-37`
```go
if frame.Length > maxFrameSize {
    return nil, errors.New("frame too large")
}
```

### 5.3 Origin 检查 - ⚠️ 需要生产环境配置
**位置**: `shell-bridge/internal/server/server.go:22-28`
```go
allowedOrigin := os.Getenv("SHELL_BRIDGE_ALLOWED_ORIGIN")
if allowedOrigin != "" {
    return r.Header.Get("Origin") == allowedOrigin
}
// Default to localhost for development
```
**建议**: 生产环境必须设置 `SHELL_BRIDGE_ALLOWED_ORIGIN`

### 5.4 WebSocket Ping - ✅ 已实现
**位置**: `websocket/handler.go:508-513`
```go
case <-pingTicker.C:
    if err := conn.WriteMessage(websocket.PingMessage, nil); err != nil {
        h.logger.Debug("Failed to send ping...")
        return
    }
```

---

## 6. 资源限制和 DoS 防护

### 6.1 速率限制 - ✅ 已实现
**位置**: `ratelimit/limiter.go`
- 三层限制：Global + Per-IP + Per-Session
- 使用 token bucket 算法
- 定期清理过期条目

### 6.2 消息大小限制 - ✅ 已实现
- RingBuffer: 1MB (buffer/ring.go:11)
- Frame: 10MB (protocol/frame.go:15)
- Upload: 可配置 (config)

### 6.3 执行超时 - ✅ 已实现
**位置**: `k8s/exec.go:74-79`
```go
if opts.Timeout > 0 {
    var cancel context.CancelFunc
    execCtx, cancel = context.WithTimeout(ctx, opts.Timeout)
    defer cancel()
}
```

### 6.4 Kubernetes 资源限制 - ✅ 已实现
**位置**: `k8s/pods.go:434-457`
- CPU/Memory requests 和 limits
- EphemeralStorage limits

---

## 7. 安全问题

### 7.1 认证 - ✅ Fail-closed 模式
**位置**: `app/app.go:323-343`
```go
if !m.authValidator.HasKeys() {
    if !m.cfg.Auth.AllowUnauthenticated {
        http.Error(w, "Service not configured...", http.StatusInternalServerError)
        return
    }
}
```
- 没有配置密钥时默认拒绝访问（dev mode 除外）

### 7.2 环境变量验证 - ✅ 已实现
**位置**: `httpapi/handlers.go:65-70`
```go
for key := range req.Env {
    if !h.cfg.ValidateEnvKey(key) {
        WriteError(w, r, ErrInvalidEnvKey, fmt.Sprintf("Invalid environment variable key: %s", key))
        return
    }
}
```

### 7.3 路径验证 - ✅ 已实现
**位置**: `httpapi/handlers.go:317-321`
```go
if !h.cfg.ValidateFilePath(dest) {
    WriteError(w, r, ErrInvalidPath, fmt.Sprintf("Invalid destination path: %s", dest))
    return
}
```

### 7.4 安全上下文 - ✅ 已实现
**位置**: `k8s/pods.go:402-431`
- RunAsNonRoot: true
- DropAllCapabilities: 可配置
- ReadOnlyRootFilesystem: 可配置

### 7.5 常量时间比较 - ✅ 已实现
**位置**: `app/app.go:348-350`
```go
// Validate service key using constant-time comparison to prevent timing attacks
// Note: authValidator.Validate() already uses crypto/subtle.ConstantTimeCompare internally
```

---

## 8. 日志和可观测性

### 8.1 结构化日志 - ✅ 良好
- 使用 `observability.Logger` 接口
- DEBUG/INFO/WARN/ERROR 级别正确使用

### 8.2 请求 ID 追踪 - ✅ 已实现
**位置**: `app/app.go:372-374`
```go
reqIDMiddleware := observability.RequestIDMiddleware(m.cfg.Server.RequestIDHeader)
v1Handler = reqIDMiddleware(v1Handler)
```

### 8.3 Metrics - ✅ 已实现
- Prometheus metrics 端点
- HTTP 请求、K8s API 失败、Sandbox 操作等都有记录

### 8.4 健康检查 - ✅ 已实现
- `/healthz` - 存活探针
- `/readyz` - 就绪探针

---

## 9. 配置管理

### 9.1 配置验证 - ✅ 已实现
**位置**: `app/app.go:71-78`
```go
validation := cfg.Validate()
if !validation.Valid {
    log.Printf("Configuration validation failed:")
    for _, e := range validation.Errors {
        log.Printf("  [%s] %s: %s", e.Code, e.FieldPath, e.Message)
    }
    log.Fatalf("Configuration validation failed (%d errors)", len(validation.Errors))
}
```

### 9.2 热重载 - ✅ 已实现
- `config.Watcher` 监控配置文件变化
- Debounce 防止频繁重载

### 9.3 默认值 - ✅ 合理
- TTL: 24小时 (session/types.go:11)
- 超时: 30秒 (WebSocket)
- 缓冲区大小: 10000 条消息 (buffer/manager.go:7)

---

## 10. 发现的问题清单

### 10.1 需要生产配置的问题

| # | 位置 | 问题描述 | 严重性 | 建议 |
|---|------|----------|--------|------|
| 1 | `shell-bridge/server.go:22-28` | 默认允许 localhost origin | 中 | 生产环境设置 `SHELL_BRIDGE_ALLOWED_ORIGIN` |

### 10.2 潜在改进（非阻塞）

| # | 位置 | 问题描述 | 严重性 | 建议 |
|---|------|----------|--------|------|
| 1 | `websocket/handler.go:516-531` | select default 分支使用 ReadDeadline 实现非阻塞读取 | 低 | 代码正确但可以更清晰地使用非阻塞模式 |
| 2 | `k8s/snapshot.go:26-27` | 硬编码 2 分钟超时 | 低 | 可以考虑配置化 |

---

## 11. 代码质量总结

### 11.1 优秀的设计模式
1. **Fail-closed 安全模式**: 认证失败默认拒绝
2. **Context 传播**: 所有操作正确传播 context
3. **资源清理**: defer 模式确保资源释放
4. **重试逻辑**: finalizer 和 snapshot 操作有指数退避重试
5. **并发安全**: 正确使用 mutex 和 sync.Map

### 11.2 测试覆盖
项目有 43+ 个测试文件，包括：
- 单元测试 (`*_test.go`)
- 集成测试 (`integration/`)
- E2E 测试 (kind cluster)

### 11.3 文档质量
- 包文档清晰
- 复杂函数有注释
- 协议定义明确

---

## 12. 上线前检查清单

### 12.1 必须检查项
- [ ] 配置 `SERVICE_KEYS` 环境变量
- [ ] 配置 `SHELL_BRIDGE_ALLOWED_ORIGIN`
- [ ] 验证 Kubernetes 集群连接
- [ ] 验证 MinIO/S3 存储配置
- [ ] 确认资源限制适合生产负载

### 12.2 推荐检查项
- [ ] 设置合理的 TTL 值
- [ ] 配置速率限制参数
- [ ] 启用 Prometheus metrics
- [ ] 配置日志级别
- [ ] 测试 finalizer 流程

---

## 13. 结论

**总体评价**: ✅ **代码质量良好，可上线**

mbos-sandbox V06 版本的代码实现质量整体良好，没有发现会影响生产环境稳定运行的明显bug。项目展现了以下优点：

1. **并发安全**: 所有共享状态都有适当的同步保护
2. **资源管理**: 正确使用 defer 确保资源释放
3. **错误处理**: 全面处理 context 取消和超时
4. **安全防护**: Fail-closed 认证、输入验证、资源限制
5. **可观测性**: 完善的日志、metrics 和健康检查

**上线建议**:
1. 确保生产环境配置正确（SERVICE_KEYS、ALLOWED_ORIGIN）
2. 进行负载测试验证资源限制配置
3. 监控 finalizer 处理性能和错误率
4. 考虑设置告警规则

**无需修复的关键问题**: 无

**可选改进项**: 文档中列出的低优先级改进项不影响上线，可在后续迭代中处理。
